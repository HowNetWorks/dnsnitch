dependencies:
  cache_directories:
    - "~/.stack/"
  pre:
    - curl -sSL https://get.haskellstack.org/ | sudo sh
  override:
    - stack update
    - stack build --only-dependencies

test:
  override:
    - ./script/test
